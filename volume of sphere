#include <stdio.h>

int main()
{
    int radius;
    float y=3.14,z;
    printf("enter the radius of sphere :");
    scanf("%d",&radius);
    z=(4*y*radius*radius*radius)/3;
    printf("volume of the sphere is:%f\n",z);

    return 0;
}
